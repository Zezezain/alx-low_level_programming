#include "main.h"
/**
 * add - sum tow numbers
 *
 * Discription: sum two numbers
 * @a: the first number
 * @b: the second one
 * Return: return 0
 */
int add(int a, int b)
{
int r = a + b;
return (r);
}
