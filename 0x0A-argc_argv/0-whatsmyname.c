#include <stdio.h>
/**
 *main - prints its name, followed by a new line
 *@argv: list of values
 *@argc: intger
 *Return: 0
 */
int main(int argc __attribute__((unused)), char const *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
