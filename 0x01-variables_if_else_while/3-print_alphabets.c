#include <stdio.h>
/**
 * main - main function
 * Return: return o
 * Discription: 'alphabet game with all letters'
 */
int main(void)
{
int no;
int noo;
for (no = 97; no <= 122; no++)
{
putchar(no);
}
for (noo = 65; noo <= 90; noo++)
{
putchar(noo);
}
putchar('\n');
return (0);
}
